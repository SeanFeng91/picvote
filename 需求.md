# 百人级图片/视频作品投票系统需求文档

## 1. 产品定位

这是一个面向内部活动、评选、作品征集或现场拉票场景的轻量级投票系统。用户用工号登录后，可以快速上传一张手机拍摄图片或一段视频，系统生成作品编号并展示在公共相册中。参与者可以按作品编号拉票、浏览作品并投票。管理员可以配置参与人员、票数规则、上传权限，并实时查看作品与排名。

产品优先满足“活动当天可用、手机端操作快、管理员可控、投票数据实时可信”的目标。

## 2. 核心目标

1. 让百人级参与者无需复杂注册，用工号快速进入系统。
2. 支持手机拍照上传、相册读取上传、视频上传。
3. 每个普通参与者最多保留 1 个作品；若要上传新作品，必须先删除原作品。
4. 每个作品自动生成唯一编号，便于线下或群内按编号拉票。
5. 每个普通用户默认有 5 票，可重复投给同一作品，也可分散投给多个作品。
6. 管理员可配置谁能上传、谁能投票、每人票数额度、特殊账号票数。
7. 管理端实时查看全部作品、票数、排名和异常状态。
8. 在电脑端和手机端都有独立适配的交互体系，而不是简单缩放同一个页面。
9. 使用 Next.js 开发前端与服务端接口，部署到 Cloudflare，D1 存储结构化数据，R2 存储媒体文件。

## 3. 角色与权限

| 角色 | 典型身份 | 权限 |
| --- | --- | --- |
| 普通参与者 | 活动参与员工 | 工号登录、上传/删除自己的 1 个作品、浏览相册、使用自己的票数投票 |
| 特殊参与者 | 嘉宾、评委、组织者指定账号 | 拥有非默认票数额度，其他能力同普通参与者或按配置扩展 |
| 管理员 | 活动组织者 | 配置账号、票数、上传/投票开关、查看实时排名、管理违规作品 |
| 展示观众 | 大屏或会议现场展示 | 只读查看排名、作品墙、票数变化，是否开放需由管理员配置 |

## 4. 业务规则

### 4.1 登录规则

- MVP 阶段采用“工号 + 活动口令/一次性密码”的轻量登录方式。
- 系统只允许已导入或管理员配置的工号登录。
- 登录后根据账号配置判断是否能上传、是否能投票、可用票数额度。
- 管理员账号与普通工号分离，管理员入口需要更强校验。

### 4.2 上传规则

- 每个可上传账号最多拥有 1 个有效作品。
- 作品类型支持图片或视频，二选一。
- 用户若已有作品，上传入口显示当前作品和“删除后重新上传”的操作。
- 删除作品后：
  - 作品从相册隐藏。
  - 该作品对应票数进入作废状态，不计入排名。
  - 投票记录保留在审计表中，便于活动争议回溯。
- 上传成功后系统生成作品编号，编号需要短、稳定、易读，例如 `A023`。
- 编号一经生成不随标题、文件或排序变化而改变。

### 4.3 投票规则

- 每个普通账号默认 5 票。
- 管理员可为单个账号配置更多或更少票数。
- 用户可以将多票投给同一个作品，也可以投给多个作品。
- 用户不能投给已删除、禁用、审核不通过或不存在的作品。
- 是否允许给自己的作品投票需要确认；默认方案为允许，降低现场解释成本。
- 投票后扣减剩余票数，并在“我的票数”中显示已投去向。
- MVP 阶段投票不可撤回；如需要撤回，需要增加操作日志和排名回滚规则。

### 4.4 管理配置规则

管理员可以配置：

- 账号清单：工号、姓名/昵称、角色。
- 上传权限：允许上传 / 禁止上传。
- 投票权限：允许投票 / 禁止投票。
- 票数额度：默认 5，可对特殊账号单独设置。
- 活动状态：未开始、上传中、投票中、已截止、结果公示。
- 作品状态：正常、隐藏、违规下架、用户已删除。

### 4.5 实时展示规则

- 管理后台需要展示当前作品总数、投票人数、已投票数、剩余票数、排名变化。
- 排名按有效票数从高到低排序。
- 同票时默认按作品编号升序排序，保证显示稳定。
- 百人级活动 MVP 可以采用短间隔刷新实现实时体验；后续如有大屏强实时需求再升级为事件推送。

## 5. 功能范围

### 5.1 用户端

- 工号登录。
- 查看活动状态和个人权限。
- 上传图片或视频。
- 查看自己的作品编号与作品状态。
- 删除自己的作品并重新上传。
- 浏览相册。
- 按作品编号搜索。
- 打开作品详情。
- 对作品投票，支持一次投 1 票或连续投多票。
- 查看剩余票数和已投记录。

### 5.2 管理端

- 管理员登录。
- 批量导入工号。
- 单个新增/编辑账号。
- 配置账号上传权限、投票权限和票数额度。
- 查看作品列表。
- 查看作品详情、上传人、上传时间、作品状态。
- 隐藏或下架作品。
- 查看实时排名。
- 查看投票统计和账号投票进度。
- 控制活动阶段。

### 5.3 展示端

- 大屏作品墙。
- 实时排名榜。
- 可选隐藏具体票数，仅展示排名。
- 可选按编号或排名切换。

## 6. 页面信息架构

### 用户端手机优先

1. `/login`：工号登录。
2. `/`：活动首页，展示我的作品、剩余票数、进入相册。
3. `/upload`：上传或替换作品。
4. `/gallery`：相册与投票主页面。
5. `/works/[code]`：作品详情与投票。
6. `/me/votes`：我的投票记录。

### 管理端电脑优先

1. `/admin/login`：管理员登录。
2. `/admin`：管理概览与实时排名。
3. `/admin/users`：账号与票数配置。
4. `/admin/works`：作品管理。
5. `/admin/votes`：投票记录与异常排查。
6. `/display`：现场展示页面。

## 7. 非功能需求

### 性能

- 支持 100-300 人同时使用的活动场景。
- 上传完成后 3 秒内在相册中可见。
- 投票后排名在 2-5 秒内刷新。
- 图片需要生成压缩展示版本，避免手机端加载原图。

### 安全

- 所有写操作必须校验登录态和权限。
- 管理接口和用户接口分权。
- 上传文件需要限制类型、大小和扩展名。
- R2 文件不直接暴露可写权限，上传必须走服务端签名或受控接口。
- 投票接口必须防重复请求和并发超投。

### 可维护性

- 业务规则集中在服务层，不散落在页面组件里。
- 数据表保留审计字段，便于活动结束后复盘。
- 配置与活动实例分离，后续可支持多场活动。

## 8. MVP 边界

第一版必须完成：

- 工号登录。
- 管理员账号登录。
- 用户上传 1 张图片或 1 段视频。
- 用户删除自己的作品并重新上传。
- 相册展示、编号搜索、作品详情。
- 默认 5 票，可重复投同一作品。
- 管理员配置账号权限和票数。
- 管理员实时排名页面。
- Cloudflare D1 + R2 持久化。
- 手机端和电脑端分别适配。

第一版暂不做：

- 复杂评委打分维度。
- 多活动并行运营。
- 评论、弹幕、社交分享。
- AI 审核。
- 投票撤回。
- 复杂反作弊模型。

## 9. 开发阶段规划

### 阶段 1：产品骨架与数据模型

- 建立 Next.js 项目结构。
- 设计 D1 表结构和 R2 文件路径。
- 完成登录、账号权限、活动状态基础能力。

### 阶段 2：用户上传与作品管理

- 完成图片/视频上传。
- 完成作品编号生成。
- 完成“每人 1 个作品”的约束。
- 完成删除与重新上传流程。

### 阶段 3：相册与投票

- 完成相册列表、编号搜索、作品详情。
- 完成投票接口、剩余票数、重复投票。
- 完成并发防超投。

### 阶段 4：管理员后台

- 完成账号配置。
- 完成作品管理。
- 完成实时排名和投票统计。
- 完成活动阶段控制。

### 阶段 5：部署与活动验收

- 接入 Cloudflare D1/R2。
- 完成生产部署。
- 完成手机端、电脑端、管理员、大屏展示验收。
- 准备活动当天操作说明和应急预案。

## 10. 验收标准

- 100 个测试工号可以登录并完成主要流程。
- 普通用户只能保留 1 个有效作品。
- 删除作品后无法继续被投票。
- 每个用户票数扣减准确，不会超投。
- 管理员修改票数后用户端立即按新额度计算。
- 排名与投票记录数据一致。
- 手机端上传、浏览、投票不需要横向滚动。
- 电脑端管理后台可以在一个页面内高密度查看排名和异常。
- 刷新页面后登录态、上传结果、投票结果不丢失。

## 11. 待确认问题

1. 登录是否只用工号，还是需要活动口令、短信码或导入的一次性密码？
2. 是否允许用户给自己的作品投票？当前默认允许。
3. 视频上传大小与时长上限是多少？建议 MVP 限制为 30-60 秒。
4. 是否需要作品审核后才公开，还是上传即公开？当前默认上传即公开，管理员可下架。
5. 活动是否有明确上传截止时间和投票截止时间？
6. 大屏展示是否允许公开展示具体票数，还是只展示排名？
7. 账号清单是否已有 Excel/CSV，需要支持批量导入模板？
8. 活动结束后是否需要导出结果和投票明细？
